/*import { Schema } from 'mariadb';

const apuestaSchema= new Schema({
    apuesta_id: String,
    created_date: Date,
    sorteo_date: Date,
    combinacion: String,
    user_id: Number
})

//export default module('Apuesta', apuestaSchema);*/
"use strict";